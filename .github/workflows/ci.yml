name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    name: Format, lint, and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy,rustfmt
      - run: cargo fmt --all -- --check
      - run: cargo clippy --all-targets --locked -- -D warnings
      - run: cargo test --all-targets --locked

  build:
    name: Build ${{ matrix.label }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            label: Windows
            gui: target/release/lumen-gui.exe
            cli: target/release/lumen.exe
          - os: macos-latest
            label: macOS
            gui: target/release/lumen-gui
            cli: target/release/lumen
          - os: ubuntu-latest
            label: Linux
            gui: target/release/lumen-gui
            cli: target/release/lumen
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo build --release --bins --locked
      - if: runner.os == 'Linux'
        run: ./target/release/lumen benchmark --strict --profile hosted-ci
      - if: runner.os == 'macOS'
        run: bash scripts/package-macos.sh
      - if: runner.os == 'Linux'
        run: bash scripts/package-linux.sh
      - if: runner.os == 'Windows'
        run: pwsh scripts/package-windows.ps1
      - uses: actions/upload-artifact@v7
        with:
          name: lumen-${{ matrix.label }}
          path: |
            ${{ matrix.gui }}
            ${{ matrix.cli }}
            target/dist/
          if-no-files-found: error
